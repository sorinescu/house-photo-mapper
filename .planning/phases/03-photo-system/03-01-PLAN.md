# Phase 3, Plan 1: Photo Models & Dependencies

**Goal:** Establish the photo domain models (PhotoModel, ExifModel, DuplicateGroup) and install required dependencies (imagehash, pillow-heif) so the photo system has a type-safe data foundation.

**Depends on:** Phase 1 (foundation complete)

---

## Tasks

### Task 1: Install new dependencies

- [ ] Add `imagehash>=4.3.2` and `pillow-heif>=1.4.0` to `pyproject.toml` dependencies
- [ ] Run `uv sync` to install into virtual environment
- [ ] Verify imports work: `python -c "import imagehash; from pillow_heif import register_heif_opener; print('OK')"`

### Task 2: Create ExifModel and PhotoModel

- [ ] Create `src/house_photo_mapper/domain/models/photo.py` with:
  - `ExifModel(BaseModel)`: timestamp, camera_make, camera_model, lens_model, orientation, gps_lat, gps_lon
  - `DuplicateGroup(BaseModel)`: group_id, photo_paths, representative_index
  - `PhotoModel(BaseModel)`: path, filename, file_size, width, height, exif, perceptual_hash, is_duplicate, duplicate_group_id, imported_at
- [ ] Add `to_project_json()` and `from_project_json()` class methods matching PlanModel pattern
- [ ] Add `__init__.py` exports in `domain/models/`

### Task 3: Register pillow-heif plugin at app startup

- [ ] In `src/house_photo_mapper/app.py`, call `register_heif_opener()` before QApplication creation
- [ ] Add import guard for pillow-heif (graceful fallback if not installed)

---

## Verification

- `uv run pytest tests/unit/test_project_model.py -x` (existing tests still pass)
- `uv run python -c "from house_photo_mapper.domain.models.photo import PhotoModel, ExifModel, DuplicateGroup; print('Models OK')"` succeeds
- `uv run python -c "from pillow_heif import register_heif_opener; register_heif_opener(); print('HEIC OK')"` succeeds

---

## Commit

`feat(03-01): add PhotoModel, ExifModel, DuplicateGroup and install imagehash/pillow-heif`
