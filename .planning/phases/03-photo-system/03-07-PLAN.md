# Phase 3, Plan 7: Photo Persistence & Phase Verification

**Goal:** Photos persist in project JSON, thumbnails survive app restart, and all Phase 3 success criteria are verified end-to-end.

**Depends on:** 03-01 through 03-06 (all photo system components)

---

## Tasks

### Task 1: Add photo persistence to PersistenceService

- [ ] In `src/house_photo_mapper/domain/services/persistence.py`:
  - `save_photo_model(photos: list[PhotoModel], project_dir: Path) -> None`: saves to `photos.json` atomically
  - `load_photo_model(project_dir: Path) -> list[PhotoModel] | None`: loads from `photos.json`
  - PhotoModel.to_project_json() already exists from 03-01
- [ ] In ProjectViewModel: wire photo persistence to save/load cycle
  - On project save: also save photos.json
  - On project load: load photos.json, populate PhotoViewModel, trigger thumbnail generation

### Task 2: Add tests for photo persistence

- [ ] Create `tests/test_photo_persistence.py`:
  - Test save/load round-trip for PhotoModel list
  - Test with EXIF data populated
  - Test with duplicate groups
  - Test empty photo list persistence

### Task 3: Phase verification — run all success criteria checks

- [ ] Criterion 1: Drag-drop adds photos to browser ✓ (verify manually)
- [ ] Criterion 2: Folder import adds all photos recursively ✓ (verify manually)
- [ ] Criterion 3: EXIF metadata displayed for each photo ✓ (verify in metadata panel)
- [ ] Criterion 4: Duplicates detected and flagged ✓ (test with duplicate images)
- [ ] Criterion 5: Thumbnails load lazily, no UI blocking ✓ (test with 100+ photos)
- [ ] Criterion 6: HEIC photos import correctly ✓ (test with .heic file if available)
- [ ] Run full test suite: `uv run pytest tests/ --cov=src/house_photo_mapper`

---

## Verification

- `uv run pytest tests/test_photo_persistence.py -x` passes
- `uv run pytest tests/ --cov=src/house_photo_mapper -x` passes with >80% coverage on new code
- Manual: create project, import photos, save, reopen — photos and thumbnails persist
- Status bar shows photo count on import

---

## Commit

`feat(03-07): add photo persistence and verify Phase 3 success criteria`
