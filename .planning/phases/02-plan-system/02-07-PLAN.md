---
phase: 02-plan-system
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-plan-system/02-UAT.md
autonomous: false
requirements: [PI-01, PI-02]
user_setup: []
gap_closure: true
must_haves:
  truths:
    - "User can import a multi-page PDF plan and see all pages in a page navigator"
    - "User can import PNG/JPG plan images and they render correctly"
  artifacts:
    - .planning/phases/02-plan-system/02-UAT.md (updated status)
  key_links:
    - "Automated tests pass for import UI"
    - "Human verification confirms UI works"
---

<objective>
Verify that the plan import UI gap closure (plan 02-06) resolved the UAT issues and update the UAT record.

Purpose: UAT tests 1 and 2 failed due to missing UI entry point. Plan 02-06 implemented the fix. This plan verifies the fix works and updates the UAT to reflect closure.
Output: Updated UAT.md with resolved status, human verification checkpoint.
</objective>

<execution_context>
@/Users/sorin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/sorin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plan-system/02-UAT.md
@.planning/phases/02-plan-system/02-06-SUMMARY.md
@.planning/phases/02-plan-system/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated import UI tests and update UAT</name>
  <files>
    .planning/phases/02-plan-system/02-UAT.md
  </files>
  <action>
1. Run the automated import UI tests to confirm they pass:
   `uv run pytest tests/test_plan_import_ui.py -x -v`

2. If tests pass, update the UAT file (.planning/phases/02-plan-system/02-UAT.md):
   - Change the status of Test 1 (Multi-Page PDF Import & Navigator) from "issue" to "pass (automated)"
   - Change the status of Test 2 (PNG/JPG Image Import) from "issue" to "pass (automated)"
   - Add a note: "Automated tests confirm import UI works. Human verification needed for visual rendering."
   - Update the Summary section: increase passed count from 5 to 7, decrease issues from 2 to 0.

3. If tests fail, investigate and fix any regressions in the import UI code.

Reference: Plan 02-06 implemented the import UI. The automated tests cover the routing logic.
Gap reason: UAT was run before plan 02-06 was executed.
  </action>
  <verify>
    <automated>uv run pytest tests/test_plan_import_ui.py -x -v</automated>
  </verify>
  <done>
    Automated import UI tests pass. UAT updated to reflect that tests 1 and 2 are resolved at the automated level.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of import UI</name>
  <files>.planning/phases/02-plan-system/02-UAT.md</files>
  <action>
Human verification of the import UI functionality. Plan 02-06 added "Import Plan..." menu action with file dialog routing PDF/images to PlanViewModel. Automated tests confirm the logic works. Now verify visually:

1. Launch the HousePhotoMapper application.
2. Go to File → Import Plan... (or press Ctrl+Shift+I).
3. Verify a file dialog opens with filter "Plans (*.pdf *.png *.jpg *.jpeg)".
4. Select a multi-page PDF file (5+ pages). Verify it loads and pages appear in the sidebar navigator.
5. Select a PNG or JPG plan image. Verify it loads and displays correctly in the viewport.
6. Cancel the dialog. Verify no error occurs.
7. Report results: type "approved" if all steps pass, or describe issues.
  </action>
  <verify>
    <human-check>Human reports "approved" or describes issues</human-check>
  </verify>
  <done>
Human confirms Import Plan menu action works with PDF and image files. UAT tests 1 and 2 are fully resolved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File dialog → PlanViewModel | User-selected file path crosses from untrusted filesystem to plan loading logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Tampering | File path from QFileDialog | medium | mitigate | PlanRenderer validates file format via PyMuPDF/Pillow; invalid files raise caught exceptions shown as user error |
| T-02-07-02 | DoS | Large file import | low | accept | PlanRenderer handles large files; tile pyramid defers heavy rendering to background (Phase 2 prior plans) |
</threat_model>

<verification>
1. `uv run pytest tests/test_plan_import_ui.py -x -v` — all import UI tests pass
2. Human verification: Import Plan menu action works, file dialog shows correct filters, PDF and image files load correctly
3. UAT updated to reflect closure
</verification>

<success_criteria>
- Automated import UI tests pass
- Human verifies Import Plan menu action works with PDF and image files
- UAT tests 1 and 2 marked as resolved
</success_criteria>

<output>
Create `.planning/phases/02-plan-system/02-07-SUMMARY.md` when done
</output>
