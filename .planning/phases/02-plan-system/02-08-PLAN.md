---
phase: 02-plan-system
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/house_photo_mapper/presentation/views/main_window.py
  - tests/test_plan_viewport_wiring.py
autonomous: false
requirements: [PI-01, PI-02]
gap_closure: true
must_haves:
  truths:
    - "User can import a multi-page PDF plan and see pages in the sidebar navigator"
    - "User can import PNG/JPG plan images and they display in the viewport"
    - "Import Plan button appears in toolbar alongside New/Open/Save"
    - "PlanView (sidebar + viewport) is the central widget after app launch"
  artifacts:
    - src/house_photo_mapper/presentation/views/main_window.py (PlanView wired in)
    - tests/test_plan_viewport_wiring.py (wiring tests)
  key_links:
    - "MainWindow creates PlanViewModel and wires to ProjectViewModel via set_plan_vm()"
    - "MainWindow._create_central_widget() instantiates PlanView with sidebar"
    - "PlanView sidebar signals connected to PlanViewModel slots"
---

<objective>
Wire PlanView and PlanViewModel into MainWindow so the plan viewport is functional.

Purpose: Plan 02-06 implemented the Import Plan menu action and file dialog routing, but PlanView was never instantiated in MainWindow._create_central_widget() (it creates a plain placeholder QWidget). ProjectViewModel.set_plan_vm() is never called, so plan_vm is always None, and import_plan() shows "No plan viewport available". The toolbar also lacks an Import Plan button.

Output: MainWindow has a working PlanView as central widget with sidebar navigation, PlanViewModel is wired to ProjectViewModel, and Import Plan button is in the toolbar.
</objective>

<execution_context>
@/Users/sorin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/sorin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plan-system/02-UAT.md
@.planning/phases/02-plan-system/02-06-SUMMARY.md
@.planning/phases/02-plan-system/02-07-SUMMARY.md
@src/house_photo_mapper/presentation/views/main_window.py
@src/house_photo_mapper/presentation/viewmodels/main_window_vm.py
@src/house_photo_mapper/presentation/viewmodels/project_vm.py
@src/house_photo_mapper/presentation/views/plan_view.py
@src/house_photo_mapper/presentation/viewmodels/plan_vm.py
@src/house_photo_mapper/presentation/views/plan_sidebar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire PlanView and PlanViewModel into MainWindow</name>
  <files>src/house_photo_mapper/presentation/views/main_window.py</files>
  <action>
Fix MainWindow to instantiate PlanView as the central widget and wire PlanViewModel to ProjectViewModel.

**1. Import PlanView, PlanViewModel, and PlanSidebar at top of file:**
Add to imports: PlanView, PlanViewModel, PlanSidebar (under TYPE_CHECKING or direct).

**2. Create PlanViewModel in __init__ and wire to ProjectViewModel:**
After creating self._project_vm, instantiate PlanViewModel and call self._project_vm.set_plan_vm(plan_vm). Store as self._plan_vm.

**3. Replace _create_central_widget() placeholder with PlanView + sidebar layout:**
Instead of creating a plain QWidget, create a PlanSidebar and PlanView. Arrange them in a QSplitter (sidebar left, plan view right). Call self.setCentralWidget(splitter). Connect sidebar signals (order_changed, floor_changed, itemClicked) to PlanViewModel slots (on_sidebar_order_changed, on_sidebar_floor_changed, on_sidebar_page_clicked). Connect PlanViewModel.pages_changed to populate sidebar. Connect PlanViewModel.pixmap_ready to sidebar thumbnail updates.

**4. Add Import Plan button to toolbar:**
After the Save button in _create_toolbar(), add self._tb_import = QAction("Import Plan", self), connect to self._vm.import_plan, add to toolbar.

**5. Connect PlanView to PlanViewModel for calibration:**
After creating PlanView, call self._plan_vm.set_plan_view(plan_view).

**Reference existing code patterns:**
- PlanView constructor takes plan_vm: PlanViewModel and parent: QWidget | None
- PlanSidebar has add_page(), order_changed Signal(list), floor_changed Signal(int, int), itemClicked Signal
- PlanViewModel has pages_changed Signal(list), pixmap_ready Signal(QPixmap), on_sidebar_order_changed, on_sidebar_floor_changed, on_sidebar_page_clicked slots
- ProjectViewModel.set_plan_vm(plan_vm) stores the reference for persistence coordination

**Gap reason:** UAT test 1 failed because _create_central_widget() creates a plain QWidget placeholder, not PlanView. set_plan_vm() is never called, so plan_vm is always None. Toolbar has no Import Plan button.
  </action>
  <verify>
    <automated>uv run pytest tests/test_plan_viewport_wiring.py -x -v</automated>
  </verify>
  <done>
PlanView is the central widget with sidebar. PlanViewModel is wired to ProjectViewModel. Import Plan button is in toolbar. User can import PDF/image and see pages in sidebar.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of plan viewport wiring</name>
  <files>.planning/phases/02-plan-system/02-UAT.md</files>
  <action>
Human verification of the complete plan viewport wiring. After Task 1 is applied:

1. Launch the HousePhotoMapper application.
2. Verify the central area shows a sidebar on the left and a plan viewport on the right (not a grey placeholder).
3. Go to File > Import Plan... (or click Import Plan in toolbar, or press Ctrl+Shift+I).
4. Select a multi-page PDF (5+ pages). Verify pages appear in the sidebar with thumbnails. Verify clicking a page switches the viewport.
5. Select a PNG/JPG image. Verify it displays in the viewport.
6. Verify Import Plan button is visible in the toolbar.
7. Report results: type "approved" if all steps pass, or describe issues.

After approval, update the UAT file:
- Change Test 1 status from "issue" to "pass"
- Change Test 2 status from "pending" to "pass"
- Update Summary: passed=2, issues=0, pending=4
  </action>
  <verify>
    <human-check>Human reports "approved" or describes issues</human-check>
  </verify>
  <done>
Human confirms PlanView displays as central widget, sidebar shows pages, import works with PDF and images, toolbar has Import Plan button. UAT tests 1 and 2 marked pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File dialog → PlanViewModel | User-selected file path crosses from untrusted filesystem to plan loading logic |
| PlanView → MainWindow layout | PlanView widget is composed into central widget, must not leak memory or crash on init |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08-01 | Tampering | File path from QFileDialog | medium | mitigate | PlanRenderer validates file format via PyMuPDF/Pillow; invalid files raise caught exceptions shown as user error |
| T-02-08-02 | DoS | Large file import | low | accept | PlanRenderer handles large files; tile pyramid defers heavy rendering to background (Phase 2 prior plans) |
| T-02-08-03 | Elevation | PlanView widget initialization | low | mitigate | PlanView and PlanViewModel are internal widgets, no external trust boundary; Qt parent ownership prevents leaks |
</threat_model>

<verification>
1. `uv run pytest tests/test_plan_viewport_wiring.py -x -v` — all wiring tests pass
2. `uv run pytest tests/test_plan_import_ui.py -x -v` — existing import UI tests still pass
3. Human verification: PlanView displays, sidebar shows pages, import works with PDF/images
</verification>

<success_criteria>
- PlanView is the central widget with sidebar navigation
- PlanViewModel is wired to ProjectViewModel via set_plan_vm()
- Import Plan button is in the toolbar
- User can import PDF/image plans and see them displayed
- UAT tests 1 and 2 marked as pass
</success_criteria>

<output>
Create `.planning/phases/02-plan-system/02-08-SUMMARY.md` when done
</output>
