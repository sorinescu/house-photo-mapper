---
phase: 01-foundation-core-architecture
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/house_photo_mapper/infrastructure/qt_patterns.py
  - src/house_photo_mapper/presentation/viewmodels/project_vm.py
  - src/house_photo_mapper/presentation/viewmodels/main_window_vm.py
  - tests/unit/test_qt_patterns.py
autonomous: true
requirements: [CP-01]
user_setup: []
must_haves:
  truths:
    - "All ViewModel @Slot methods use @Slot() decorator (not lambda)"
    - "All QObject subclasses set parent in __init__ for automatic cleanup"
    - "QRunnable subclasses use setAutoDelete(False) and explicit deleteLater()"
    - "CallableSlotAdapter pattern used for any lambda/closure signal connections"
    - "No memory leaks detected by pytest-qt leak detection (if available)"
    - "Ruff rule PYI024 (pyqt-slot) enforced in pre-commit"
    - "MyPy strict mode passes with no Qt-related errors"
  artifacts:
    - src/house_photo_mapper/infrastructure/qt_patterns.py (base classes, adapters)
    - Updated project_vm.py and main_window_vm.py using patterns
    - tests/unit/test_qt_patterns.py (pattern compliance tests)
  key_links:
    - "All ViewModels inherit from QtSafeViewModel base class"
    - "All background tasks use QtSafeRunnable base class"
    - "CallableSlotAdapter used for any non-@Slot connections"
---

<objective>
Establish and enforce PySide6 memory-safe patterns across the codebase: @Slot() on all signal handlers, parented QObject hierarchies, QRunnable auto-delete management, and CallableSlotAdapter for closures. Create reusable base classes in infrastructure/qt_patterns.py. Refactor ProjectViewModel and MainWindowViewModel to use these patterns. Add Ruff and MyPy rules to catch violations.
Purpose: Prevents segfaults, memory leaks, and object lifetime bugs that are notoriously hard to debug in PySide6. These patterns are mandatory per RESEARCH.md Pitfalls #1, #5.
Output: Pattern library + compliant ViewModels + automated enforcement via linting.
</objective>

<execution_context>
@/Users/sorin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/sorin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-core-architecture/01-RESEARCH.md
@.planning/phases/01-foundation-core-architecture/01-VALIDATION.md
@.planning/phases/01-foundation-core-architecture/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Qt pattern base classes and adapters</name>
  <files>
    src/house_photo_mapper/infrastructure/qt_patterns.py
    tests/unit/test_qt_patterns.py
  </files>
  <behavior>
    - QtSafeViewModel(QObject): base for all ViewModels; enforces parent in __init__; provides safe_signal_connect helper
    - QtSafeRunnable(QRunnable): base for background tasks; setAutoDelete(False); stores parent reference; finished signal calls deleteLater()
    - CallableSlotAdapter(QObject): wraps any callable as @Slot(); takes parent for lifetime management
    - safe_connect(sender, signal, receiver, slot): utility that wraps slot in CallableSlotAdapter if not already @Slot()
    - All classes have comprehensive docstrings explaining memory safety rationale
  </behavior>
  <action>
    Create qt_patterns.py with the three base classes per RESEARCH.md Pitfall #1 and #5. QtSafeViewModel.__init__(parent=None) calls super().__init__(parent). QtSafeRunnable.__init__() calls setAutoDelete(False), stores parent. safe_connect inspects slot for @Slot decorator; if missing, wraps in CallableSlotAdapter(parent=receiver). Write tests verifying: parent-child cleanup, auto-delete disabled, adapter wraps callable, signal fires correctly through adapter.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_qt_patterns.py -x -v</automated>
  </verify>
  <done>
    QtSafeViewModel, QtSafeRunnable, CallableSlotAdapter, safe_connect implemented and tested
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor ProjectViewModel and MainWindowViewModel to use safe patterns</name>
  <files>
    src/house_photo_mapper/presentation/viewmodels/project_vm.py
    src/house_photo_mapper/presentation/viewmodels/main_window_vm.py
    tests/unit/test_qt_patterns.py
  </files>
  <behavior>
    - ProjectViewModel inherits QtSafeViewModel
    - All @Slot methods keep decorators; no lambda connections in __init__
    - Any signal connection in ViewModel uses safe_connect helper
    - MainWindowViewModel inherits QtSafeViewModel
    - Background operations (if any) use QtSafeRunnable subclasses
    - Parent passed correctly in construction chain: MainWindow → MainWindowViewModel → ProjectViewModel
  </behavior>
  <action>
    Update project_vm.py: change class ProjectViewModel(QtSafeViewModel). In __init__, accept parent and pass to super(). Replace any direct signal.connect with safe_connect. Update main_window_vm.py similarly. Ensure construction order in main_window.py: MainWindow creates MainWindowViewModel(parent=window), which creates ProjectViewModel(parent=vm). Add tests verifying parent chain and no lambda connections.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_qt_patterns.py::TestViewModelPatterns -x -v</automated>
  </verify>
  <done>
    Both ViewModels use QtSafeViewModel base, parent chain correct, no raw lambda connections
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Ruff pyqt-slot rule and MyPy strict config for Qt</name>
  <files>
    pyproject.toml
    tests/unit/test_qt_patterns.py
  </files>
  <action>
    In pyproject.toml [tool.ruff], add "pyqt-slot" to select list (requires ruff >= 0.5). Add [tool.ruff.lint.pyqt-slot] config if needed. In [tool.mypy], ensure strict = true and add PySide6 stubs if missing. Run ruff check and mypy to verify no violations in viewmodels. Add test that runs ruff check src/house_photo_mapper/presentation/viewmodels/ programmatically and asserts zero violations.
  </action>
  <verify>
    <automated>uv run ruff check src/house_photo_mapper/presentation/viewmodels/ && uv run mypy src/house_photo_mapper/presentation/viewmodels/</automated>
  </verify>
  <done>
    Ruff pyqt-slot rule passes, MyPy strict passes on ViewModel code, automated test enforces this
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → ViewModel slots | Slots receive user actions; must not crash or leak on malformed input |
| Background thread → Main thread | QRunnable results passed via signals; thread safety required |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-12 | Denial of Service | QRunnable auto-delete race | high | mitigate | QtSafeRunnable.setAutoDelete(False) + explicit deleteLater() in finished handler |
| T-1-13 | Denial of Service | Lambda signal connection leak | medium | mitigate | CallableSlotAdapter + safe_connect wrapper; Ruff pyqt-slot rule catches missing @Slot |
| T-1-14 | Elevation of Privilege | Parent-less QObject cleanup | low | accept | Qt parent hierarchy ensures cleanup; app shutdown cleans top-level |

</threat_model>

<verification>
- [ ] `uv run pytest tests/unit/test_qt_patterns.py -x -v` all pass
- [ ] `uv run ruff check src/house_photo_mapper/presentation/viewmodels/` zero violations
- [ ] `uv run mypy src/house_photo_mapper/presentation/viewmodels/` zero errors
- [ ] Manual: Run app, open/close multiple projects, verify no memory growth (Activity Monitor)
- [ ] Manual: Force GC after operations, verify no dangling QObject warnings
</verification>

<success_criteria>
All pattern tests pass. Ruff and MyPy enforce memory-safe patterns on ViewModels. Parent-child hierarchy correct. No lambda connections in ViewModel code. CP-01 memory safety foundation established.
</success_criteria>

<output>
Create .planning/phases/01-foundation-core-architecture/01-04-SUMMARY.md when done
</output>