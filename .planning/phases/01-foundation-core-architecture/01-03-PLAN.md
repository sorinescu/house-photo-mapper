---
phase: 01-foundation-core-architecture
plan: 03
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - src/house_photo_mapper/domain/models/coordinate.py
  - src/house_photo_mapper/domain/services/coordinate.py
  - tests/unit/test_coordinate.py
autonomous: true
requirements: [CP-01]
user_setup: []
must_haves:
  truths:
    - "CoordinateSystem enum defines WORLD (Y-up), SCREEN (Y-down), EXIF (8 orientations)"
    - "WorldPoint and ScreenPoint dataclasses are immutable (frozen, slots)"
    - "CoordinateConverter.world_to_screen() flips Y axis correctly"
    - "CoordinateConverter.screen_to_world() flips Y axis correctly"
    - "CoordinateConverter.exif_to_world() handles all 8 EXIF orientations per TIFF spec"
    - "CRSMismatchError raised when transforming between incompatible coordinate systems"
    - "All conversions are unit-tested with round-trip verification"
    - "Converter is stateless and thread-safe (no mutable instance state)"
  artifacts:
    - src/house_photo_mapper/domain/models/coordinate.py
    - src/house_photo_mapper/domain/services/coordinate.py
    - tests/unit/test_coordinate.py (fully implemented)
  key_links:
    - "CoordinateConverter injected into ViewModels needing geometry transforms"
    - "WorldPoint used by PlanModel (Phase 2) for annotations"
    - "ScreenPoint used by PlanView for mouse coordinates"
    - "EXIF orientation consumed by PhotoModel (Phase 3)"
---

<objective>
Implement the coordinate system foundation: CoordinateSystem enum (WORLD, SCREEN, EXIF), WorldPoint/ScreenPoint immutable dataclasses, CoordinateConverter service with pixels_per_meter scaling, and CRSMismatchError for invalid transforms. All 8 EXIF orientations handled per TIFF spec. 100% unit test coverage with round-trip tests.
Purpose: Provides the single source of truth for all coordinate transformations across Plan (Phase 2), Photo (Phase 3), Annotation (Phase 4), and Report (Phase 6) systems. Prevents Y-up/Y-down bugs that cascade through the application.
Output: Pure-Python, Qt-free coordinate library with comprehensive test suite.
</objective>

<execution_context>
@/Users/sorin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/sorin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-core-architecture/01-RESEARCH.md
@.planning/phases/01-foundation-core-architecture/01-VALIDATION.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Implement CoordinateSystem enum, point dataclasses, and CRSMismatchError</name>
  <files>
    src/house_photo_mapper/domain/models/coordinate.py
    tests/unit/test_coordinate.py
  </files>
  <behavior>
    - CoordinateSystem enum has WORLD, SCREEN, EXIF members
    - WorldPoint(x: float, y: float) frozen dataclass with slots, Y-up semantics
    - ScreenPoint(x: float, y: float) frozen dataclass with slots, Y-down semantics
    - CRSMismatchError(ValueError) raised with descriptive message for invalid transforms
    - Enum and dataclasses have no Qt dependencies
  </behavior>
  <action>
    Create coordinate.py with CoordinateSystem(Enum), WorldPoint, ScreenPoint, CRSMismatchError per RESEARCH.md example. WorldPoint and ScreenPoint use @dataclass(frozen=True, slots=True). Add __repr__ for debugging. Write tests for enum values, point creation, immutability (frozen), and CRSMismatchError inheritance.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_coordinate.py::TestCoordinateModels -x -v</automated>
  </verify>
  <done>
    CoordinateSystem enum, WorldPoint, ScreenPoint, CRSMismatchError defined and tested
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement CoordinateConverter with WORLD↔SCREEN transforms</name>
  <files>
    src/house_photo_mapper/domain/services/coordinate.py
    tests/unit/test_coordinate.py
  </files>
  <behavior>
    - CoordinateConverter(pixels_per_meter=100.0) stores scale factor
    - world_to_screen(WorldPoint, viewport_origin: ScreenPoint) → ScreenPoint: x_scaled = x * ppm + origin.x, y_scaled = -y * ppm + origin.y (Y flip)
    - screen_to_world(ScreenPoint, viewport_origin: ScreenPoint) → WorldPoint: x_world = (x - origin.x) / ppm, y_world = -(y - origin.y) / ppm (Y flip)
    - Round-trip: world → screen → world returns original WorldPoint (within float epsilon)
    - Viewport origin represents pan offset in screen coordinates
  </behavior>
  <action>
    Implement CoordinateConverter class with world_to_screen and screen_to_world methods. Use viewport_origin ScreenPoint for pan offset. Y-axis flip achieved by negating y * ppm in world→screen and negating (y - origin.y) / ppm in screen→world. Write parameterized tests for multiple viewport origins, scale factors, and round-trip verification.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_coordinate.py::TestWorldScreenTransform -x -v</automated>
  </verify>
  <done>
    World↔Screen transforms work correctly with Y-axis flip, round-trip passes, pan offset handled
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Implement EXIF orientation handling (all 8 orientations)</name>
  <files>
    src/house_photo_mapper/domain/services/coordinate.py
    tests/unit/test_coordinate.py
  </files>
  <behavior>
    - exif_to_world(screen_pt: ScreenPoint, orientation: int, image_size: tuple[int, int]) → WorldPoint
    - Handles EXIF orientations 1-8 per TIFF/EXIF spec:
      1: identity, 2: flip H, 3: rotate 180, 4: flip V, 5: transpose, 6: rotate 90 CW, 7: transverse, 8: rotate 270 CW
    - Raises CRSMismatchError for orientation not in 1..8
    - Applies orientation transform in image pixel space, then delegates to screen_to_world
    - Round-trip: for each orientation, exif_to_world(screen_to_exif(world)) ≈ original world (within float precision)
  </behavior>
  <action>
    Implement exif_to_world method with match/case on orientation 1-8 per RESEARCH.md code example. Transform screen point within image bounds (0..w, 0..h) according to orientation, then call screen_to_world with zero viewport origin. Write parameterized tests for all 8 orientations with known input/output coordinates. Test invalid orientation raises CRSMismatchError.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_coordinate.py::TestEXIFTransform -x -v</automated>
  </verify>
  <done>
    All 8 EXIF orientations transform correctly, round-trip verified, invalid orientation raises CRSMismatchError
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EXIF data → CoordinateConverter | Photo EXIF orientation from untrusted user files; malicious values could cause exceptions |
| Viewport origin → CoordinateConverter | Pan offset from user interaction; extreme values could cause float overflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-09 | Denial of Service | CoordinateConverter.exif_to_world | low | mitigate | Validate orientation ∈ [1,8]; raise CRSMismatchError with clear message; no crash |
| T-1-10 | Tampering | CoordinateConverter scale factor | low | accept | pixels_per_meter is constructor arg; not user-modifiable at runtime |
| T-1-11 | Information Disclosure | Coordinate precision | low | accept | Float64 precision sufficient for architectural scales; no sensitive data |

</threat_model>

<verification>
- [ ] `uv run pytest tests/unit/test_coordinate.py -x -v` all tests pass (target: 20+ tests)
- [ ] Coverage: `uv run pytest tests/unit/test_coordinate.py --cov=src/house_photo_mapper/domain --cov-report=term-missing` shows >95%
- [ ] No Qt imports in coordinate.py or services/coordinate.py (pure Python)
- [ ] CRSMismatchError raised for invalid orientation, caught and handled in tests
</verification>

<success_criteria>
All coordinate system tests pass with >95% coverage. CoordinateConverter is pure Python, thread-safe, stateless. WORLD↔SCREEN round-trip exact. All 8 EXIF orientations correct per TIFF spec. CP-01 coordinate foundation ready for Phase 2+ consumers.
</success_criteria>

<output>
Create .planning/phases/01-foundation-core-architecture/01-03-SUMMARY.md when done
</output>