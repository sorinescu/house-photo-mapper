---
phase: 04-annotation-tools
plan: 04
type: execute
wave: 2
depends_on:
  - 04-03
files_modified:
  - src/house_photo_mapper/presentation/views/main_window.py
  - src/house_photo_mapper/presentation/views/annotation_toolbar.py
  - src/house_photo_mapper/presentation/views/annotation_properties_panel.py
  - tests/unit/test_shortcuts.py
autonomous: true
requirements:
  - NA-01
  - NA-02
  - NA-03
  - NA-04
  - NA-05
  - NA-06
  - NA-07
  - NA-08
  - US-02
must_haves:
  truths:
    - Arrow keys navigate between photos in browser
    - Space key confirms annotation placement
    - Ctrl+Z undoes last action
    - Ctrl+Y redoes last undone action
    - Delete key removes selected annotation
    - Professional keyboard shortcuts work in all contexts
    - Annotation toolbar shows current tool state
    - Properties panel shows/edits annotation metadata
  artifacts:
    - src/house_photo_mapper/presentation/views/annotation_toolbar.py
    - src/house_photo_mapper/presentation/views/annotation_properties_panel.py
    - tests/unit/test_shortcuts.py
  key_links:
    - QShortcut integration with AnnotationViewModel
    - Toolbar tool state synchronization
    - Properties panel metadata editing
---

<objective>
Implement keyboard shortcuts for all annotation operations and navigation, create annotation toolbar for tool selection, and build properties panel for metadata editing. Shortcuts use QShortcut with proper context to avoid conflicts with existing handlers.

Purpose: Provide professional keyboard shortcuts for efficient annotation workflow, with visual toolbar for tool selection and properties panel for metadata editing.

Output: Complete keyboard shortcut infrastructure, annotation toolbar, and properties panel integrated with MainWindow and AnnotationViewModel.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@.planning/phases/04-annotation-tools/04-RESEARCH.md
@.planning/phases/04-annotation-tools/04-PATTERNS.md
@src/house_photo_mapper/presentation/views/main_window.py
@src/house_photo_mapper/presentation/viewmodels/annotation_vm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Keyboard Shortcuts</name>
  <files>
    src/house_photo_mapper/presentation/views/main_window.py,
    tests/unit/test_shortcuts.py
  </files>
  <action>
Implement keyboard shortcuts in MainWindow using QShortcut per D-NA-01 through D-NA-08 and D-US-02.

1. Add QShortcut setup method in MainWindow.__init__:
   - Undo: QKeySequence.StandardKey.Undo → annotation_vm.undo_stack.undo
   - Redo: QKeySequence.StandardKey.Redo → annotation_vm.undo_stack.redo
   - Save: QKeySequence.StandardKey.Save → vm.save_project (already exists, verify)
   - Delete: QKeySequence(Qt.Key.Key_Delete) → annotation_vm.delete_selected
   - Left/Up: QKeySequence(Qt.Key.Key_Left/Key_Up) → photo_vm.navigate_previous
   - Right/Down: QKeySequence(Qt.Key.Key_Right/Key_Down) → photo_vm.navigate_next
   - Space: QKeySequence(Qt.Key.Key_Space) → annotation_vm.complete_creation
   - Escape: QKeySequence(Qt.Key.Key_Escape) → annotation_vm.cancel_creation

2. Add focus-aware shortcut handling:
   - Check if QLineEdit/QTextEdit has focus before processing arrow shortcuts
   - Disable photo navigation shortcuts when text field has focus
   - Use QShortcut.setEnabled() based on focus state

3. Wire existing Edit menu undo/redo actions to QUndoStack:
   - Connect undo_action to annotation_vm.undo_stack.undo
   - Connect redo_action to annotation_vm.undo_stack.redo
   - Enable/disable actions based on stack state

4. Create test file `tests/unit/test_shortcuts.py` with:
   - TestUndoRedoShortcuts: Ctrl+Z/Ctrl+Y trigger undo/redo
   - TestDeleteShortcut: Delete key removes selected annotation
   - TestArrowNavigation: Left/Right keys navigate photos
   - TestSpaceConfirmation: Space key confirms placement
   - TestEscapeCancellation: Escape key cancels creation
   - TestFocusAwareness: Shortcuts disabled when text field has focus

Per D-NA-01 through D-NA-08: All navigation shortcuts must work correctly.
Per D-US-02: Professional keyboard shortcuts must be ergonomic and conflict-free.
  </action>
  <verify>
    <automated>cd /Users/sorin/src/my/house-photo-mapper && uv run pytest tests/unit/test_shortcuts.py -x -v</automated>
  </verify>
  <done>
    - All keyboard shortcuts implemented with QShortcut
    - Shortcuts are focus-aware (disabled during text editing)
    - Edit menu undo/redo actions wired to QUndoStack
    - Tests pass for all shortcut behaviors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Annotation Toolbar and Properties Panel</name>
  <files>
    src/house_photo_mapper/presentation/views/annotation_toolbar.py,
    src/house_photo_mapper/presentation/views/annotation_properties_panel.py,
    src/house_photo_mapper/presentation/views/main_window.py
  </files>
  <action>
Create annotation toolbar and properties panel per D-NA-02 and D-US-01.

1. Create `src/house_photo_mapper/presentation/views/annotation_toolbar.py`:
   - AnnotationToolbar(QWidget) with QToolBar containing tool toggle actions
   - Actions: Select, Place Marker, Draw Polygon
   - Connect actions to AnnotationViewModel.set_tool()
   - Use checked/unchecked state to reflect current tool
   - Style with icons and tooltips

2. Create `src/house_photo_mapper/presentation/views/annotation_properties_panel.py`:
   - AnnotationPropertiesPanel(QWidget) for metadata editing
   - Title field (QLineEdit, required)
   - Description field (QTextEdit, optional)
   - Tags field (QLineEdit, comma-separated)
   - Floor selector (QSpinBox, -2 to 10)
   - Connect to AnnotationViewModel for selected annotation
   - Update fields when annotation selection changes
   - Save changes when user edits fields

3. Integrate in MainWindow:
   - Create AnnotationToolbar instance
   - Create AnnotationPropertiesPanel instance
   - Add toolbar to toolbar area
   - Add properties panel to right dock area
   - Connect to AnnotationViewModel signals

4. Add tests for toolbar and panel:
   - TestToolbarToolSelection: clicking tools changes AnnotationViewModel state
   - TestPropertiesPanelMetadata: editing fields updates annotation
   - TestPropertiesPanelSelection: selecting annotation shows its metadata

Per D-NA-02: Space key confirms placement (already in shortcuts).
Per D-US-01: User can annotate in ≤3 clicks (toolbar facilitates this).
  </action>
  <verify>
    <automated>cd /Users/sorin/src/my/house-photo-mapper && uv run pytest tests/unit/test_shortcuts.py -x -v</automated>
  </verify>
  <done>
    - Annotation toolbar shows tool selection with current state
    - Properties panel displays/edits annotation metadata
    - Toolbar and panel integrate with MainWindow layout
    - Tests pass for toolbar and panel functionality
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard input → QShortcut | User key presses trigger shortcut actions |
| Toolbar click → AnnotationViewModel | Tool selection changes ViewModel state |
| Properties panel → AnnotationModel | Metadata edits modify annotation data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | Keyboard shortcut injection | low | mitigate | QShortcut context limits shortcuts to active window |
| T-04-05 | DoS | Rapid shortcut activation | low | accept | Qt event loop handles rapid inputs gracefully |
| T-04-06 | Elevation | Toolbar state manipulation | low | mitigate | Toolbar state synchronized with AnnotationViewModel |
</threat_model>

<verification>
- All keyboard shortcuts work correctly
- Shortcuts are disabled during text editing
- Annotation toolbar shows current tool state
- Properties panel displays/edits metadata
- Tests pass for all shortcut and UI behaviors
</verification>

<success_criteria>
- Professional keyboard shortcuts work in all contexts
- Annotation toolbar provides visual tool selection
- Properties panel enables metadata editing
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/04-annotation-tools/04-04-SUMMARY.md` when done
</output>
