---
phase: 04-annotation-tools
plan: 05
type: execute
wave: 3
depends_on:
  - 04-04
files_modified:
  - src/house_photo_mapper/domain/models/photo.py
  - src/house_photo_mapper/domain/services/persistence.py
  - src/house_photo_mapper/presentation/viewmodels/annotation_vm.py
  - src/house_photo_mapper/presentation/viewmodels/photo_vm.py
  - src/house_photo_mapper/presentation/views/main_window.py
  - tests/integration/test_photo_annotation_binding.py
  - tests/integration/test_annotation_flow.py
autonomous: true
requirements:
  - NA-01
  - NA-02
  - US-01
must_haves:
  truths:
    - Photo selection highlights associated annotation on plan
    - Annotation selection highlights associated photo in browser
    - Annotation placement creates photo binding
    - Annotation removal clears photo binding
    - Arrow keys navigate between annotated photos
    - User can complete annotation in ≤3 clicks
    - Annotations persist across save/load
  artifacts:
    - src/house_photo_mapper/domain/models/photo.py (updated)
    - src/house_photo_mapper/domain/services/persistence.py (updated)
    - tests/integration/test_photo_annotation_binding.py
    - tests/integration/test_annotation_flow.py
  key_links:
    - Bidirectional selection sync between PhotoViewModel and AnnotationViewModel
    - Photo-annotation binding via annotation_id field
    - Annotation persistence with atomic writes
---

<objective>
Bind photos to annotations with bidirectional selection sync, implement annotation placement workflow, and add annotation persistence. Photos gain annotation_id field, and selection sync ensures clicking a photo highlights its annotation on the plan and vice versa.

Purpose: Complete the annotation workflow by binding photos to annotations and enabling seamless navigation between photo browser and plan annotations.

Output: Photo-annotation binding with bidirectional selection sync, annotation persistence, and complete annotation workflow.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@.planning/phases/04-annotation-tools/04-RESEARCH.md
@.planning/phases/04-annotation-tools/04-PATTERNS.md
@src/house_photo_mapper/domain/models/photo.py
@src/house_photo_mapper/domain/services/persistence.py
@src/house_photo_mapper/presentation/viewmodels/annotation_vm.py
@src/house_photo_mapper/presentation/viewmodels/photo_vm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Photo-Annotation Binding and Persistence</name>
  <files>
    src/house_photo_mapper/domain/models/photo.py,
    src/house_photo_mapper/domain/services/persistence.py,
    tests/integration/test_photo_annotation_binding.py
  </files>
  <action>
Add annotation_id field to PhotoModel and implement annotation persistence per D-NA-01, D-NA-02, and D-US-01.

1. Update PhotoModel in `src/house_photo_mapper/domain/models/photo.py`:
   - Add field: annotation_id: Optional[str] = Field(default=None, description="Bound annotation ID")
   - Serialization is automatic via model_dump(mode="json")
   - from_project_json() handles optional default

2. Add annotation persistence to PersistenceService:
   - save_annotation_model(annotations: list[AnnotationModel], project_dir: Path)
   - load_annotation_model(project_dir: Path) -> list[AnnotationModel] | None
   - Use same atomic write pattern as save_photo_model (tmp → rename)
   - Save to project_dir/annotations.json

3. Create test file `tests/integration/test_photo_annotation_binding.py`:
   - TestPhotoAnnotationBinding: photo gains annotation_id when annotation created
   - TestAnnotationPersistence: annotations save/load correctly
   - TestPhotoAnnotationSync: photo selection syncs with annotation selection
   - TestBidirectionalSelection: annotation selection syncs with photo selection

Per D-NA-01: Arrow keys navigate between annotated photos.
Per D-NA-02: Space key confirms annotation placement.
Per D-US-01: User can annotate in ≤3 clicks.
  </action>
  <verify>
    <automated>cd /Users/sorin/src/my/house-photo-mapper && uv run pytest tests/integration/test_photo_annotation_binding.py -x -v</automated>
  </verify>
  <done>
    - PhotoModel has annotation_id field
    - PersistenceService can save/load annotations
    - Photo-annotation binding works correctly
    - Tests pass for binding and persistence
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Bidirectional Selection Sync</name>
  <files>
    src/house_photo_mapper/presentation/viewmodels/annotation_vm.py,
    src/house_photo_mapper/presentation/viewmodels/photo_vm.py,
    src/house_photo_mapper/presentation/views/main_window.py,
    tests/integration/test_photo_annotation_binding.py,
    tests/integration/test_annotation_flow.py
  </files>
  <action>
Implement bidirectional selection sync between photo browser and plan annotations per D-NA-01, D-NA-02, and D-US-01.

1. Add signals to AnnotationViewModel:
   - annotation_selected = Signal(object)  # already exists
   - Add select_annotation(annotation_id) method

2. Add signals to PhotoViewModel:
   - selection_changed = Signal(object)  # already exists
   - Add navigate_previous() and navigate_next() methods

3. Implement bidirectional sync in MainWindow:
   - Connect photo_vm.selection_changed to annotation_vm.select_annotation
   - Connect annotation_vm.annotation_selected to photo_vm.select_photo
   - When photo selected: if photo has annotation_id, select that annotation
   - When annotation selected: if annotation has photo_path, select that photo

4. Implement annotation placement workflow:
   - User selects photo in browser
   - User clicks on plan to place annotation
   - Annotation is created with photo binding (photo.annotation_id = annotation.annotation_id)
   - Photo is marked as annotated

5. Implement annotation removal workflow:
   - User selects annotation on plan
   - User presses Delete
   - Annotation is removed via undo stack
   - Photo binding is cleared (photo.annotation_id = None)

6. Implement annotation navigation:
   - Left/Right arrow keys navigate between annotated photos
   - Show annotation on plan when photo is selected
   - Scroll plan view to show annotation

7. Create test file `tests/integration/test_annotation_flow.py`:
   - TestThreeClickAnnotation: complete annotation in ≤3 clicks
   - TestPhotoAnnotationWorkflow: select photo → place annotation → binding created
   - TestAnnotationRemovalWorkflow: delete annotation → binding cleared
   - TestAnnotationNavigation: arrow keys navigate annotated photos

Per D-NA-01: Arrow keys navigate previous/next photo.
Per D-NA-02: Space key confirms/places annotation.
Per D-US-01: User can annotate a photo in ≤3 clicks.
  </action>
  <verify>
    <automated>cd /Users/sorin/src/my/house-photo-mapper && uv run pytest tests/integration/test_annotation_flow.py -x -v</automated>
  </verify>
  <done>
    - Bidirectional selection sync works between photo browser and plan
    - Annotation placement creates photo binding
    - Annotation removal clears photo binding
    - Arrow keys navigate annotated photos
    - User can complete annotation in ≤3 clicks
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Photo selection → Annotation selection | User clicks in photo browser trigger annotation highlight |
| Annotation selection → Photo selection | User clicks on plan trigger photo browser highlight |
| Annotation creation → Photo binding | Creating annotation links to photo via annotation_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | Annotation binding manipulation | low | mitigate | Binding only created through proper workflow |
| T-04-08 | DoS | Rapid selection sync | low | accept | Qt signal/slot handles rapid signals gracefully |
| T-04-09 | Information | Annotation data exposure | low | accept | Annotations are local to project, no external exposure |
</threat_model>

<verification>
- Bidirectional selection sync works correctly
- Photo-annotation binding created on annotation placement
- Photo-annotation binding cleared on annotation removal
- Arrow keys navigate annotated photos
- Annotation workflow completes in ≤3 clicks
- Tests pass for all binding and sync behaviors
</verification>

<success_criteria>
- Photo selection highlights annotation on plan
- Annotation selection highlights photo in browser
- Annotation placement creates binding
- Annotation removal clears binding
- Arrow keys navigate annotated photos
- ≤3 click annotation workflow
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/04-annotation-tools/04-05-SUMMARY.md` when done
</output>
